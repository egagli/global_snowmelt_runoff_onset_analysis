[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "gsro-analysis"
version = "0.1.0"
description = "Analysis utilities for the global snowmelt runoff onset dataset"
authors = [{ name = "Eric Gagliano", email = "egagli@uw.edu" }]
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["gsro_analysis"]
